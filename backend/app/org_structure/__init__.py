"""
Organization Structure module.

Departments, Positions, and every employee<->department / employee<->position /
department-leadership / employee-reporting relationship table. Deliberately
independent of ``app.rbac`` (software permissions) -- see
``app.org_structure.models`` module docstring.
"""