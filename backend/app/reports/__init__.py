"""
Reports Module (Reserved).

Intentionally empty in Phase 1, per scope. Will provide cross-module,
read-oriented reporting/aggregation endpoints in a later phase (e.g.
headcount by department, org charts), reading from other modules'
repositories rather than owning primary data itself.
"""
