"""
Departments Module.

Owns the ``Department`` ORM model and department CRUD: self-referential
parent/child hierarchy, an optional manager (an ``Employee``), and the
standard model/schemas/repository/service/routes layout used by every
other feature module.
"""
